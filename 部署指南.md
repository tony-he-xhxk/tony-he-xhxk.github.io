# 部署指南：把个人主页推送到 GitHub Pages

> 适用：`D:\MyHomePage` 里的站点文件 → GitHub 仓库 `tony-he-xhxk.github.io` → 上线  
> 环境：Windows + Git Bash（你之前 clone 过仓库，说明 Git 已装好）  
> 全程约 5 分钟。第 1、3 步是网页操作，第 2 步敲命令。

---

## 第 1 步：新建 GitHub 仓库（网页操作，约 1 分钟）

1. 登录 [github.com](https://github.com)，右上角 **+ → New repository**
2. **Repository name** 填：`tony-he-xhxk.github.io`  
   （必须和用户名完全一致 + `.github.io`，这是 GitHub Pages 用户主页的硬性约定）
3. **Public**（必须选公开，GitHub Pages 免费用户只支持公开仓库）
4. **不要勾选** "Add a README file"（避免和本地文件冲突）
5. 点 **Create repository**，完成（不要关页面，下一步要用它的地址）

---

## 第 2 步：本地推送文件（Git Bash，约 2 分钟）

打开 Git Bash，逐条执行：

```bash
# 1. 进入站点目录
cd /d/MyHomePage

# 2. 检查 git 身份（只执行一次；如果以前配过会直接显示已有名字）
git config --global user.name "tony-he-xhxk"
git config --global user.email "你的GitHub邮箱"

# 3. 初始化并提交
git init
git add .
git commit -m "init: 个人主页站点"

# 4. 分支名统一为 main
git branch -M main

# 5. 关联远程仓库（地址就是第 1 步那个仓库）
git remote add origin https://github.com/tony-he-xhxk/tony-he-xhxk.github.io.git

# 6. 推送
git push -u origin main
```

### 如果 push 时弹窗要登录（重点）

GitHub 已不支持用密码 push，**密码框里填 Personal Access Token（PAT）**：

1. GitHub → 右上角头像 → **Settings → Developer settings → Personal access tokens → Tokens (classic)**
2. 点 **Generate new token (classic)**，勾选 `repo` 权限（第一个大勾）
3. 有效期选 90 天足够，生成后**立刻复制**（只显示一次）
4. push 弹窗时：Username 填 `tony-he-xhxk`，Password 填**这串 token**，勾选记住凭据

> 如果电脑装了 GitHub CLI（`gh`），也可以先 `gh auth login` 登录，push 就不用输凭据了。

---

## 第 3 步：开启 GitHub Pages（网页操作，约 1 分钟）

1. 打开仓库 `tony-he-xhxk.github.io` → **Settings** → 左侧菜单 **Pages**
2. **Source** 选择：`Deploy from a branch`
3. **Branch** 选 `main`，目录选 `/(root)` → 点 **Save**
4. 等 1–2 分钟（顶部会出现 "Your site is live at ..." 提示，构建完成后刷新可见）

---

## 第 4 步：验证上线

浏览器访问 **`https://tony-he-xhxk.github.io`**：

- [ ] 首页：薄荷绿背景 + Hero（星河绫）+ 「最近帖子」两篇卡片
- [ ] 点击帖子卡片 → 进入详情页（标题/日期/标签/正文）
- [ ] 详情页「返回首页」可用
- [ ] 地址栏 HTTPS 自动生效（github.io 免费证书，无需配置）
- [ ] 手机浏览器打开也正常（响应式）

全部 ✅ 就成功了。之后每写一篇新帖子：`_posts/` 下加一个 `.md` 文件 → `git add . && git commit -m "..." && git push` 即可更新。

---

## 常见问题排查

| 问题                                      | 解法                                                                           |
| --------------------------------------- | ---------------------------------------------------------------------------- |
| `push` 报 `Updates were rejected`（远程有内容） | 建仓库时勾了 README。执行 `git pull origin main --rebase` 再 `git push -u origin main` |
| 访问显示 404                                | 等 1–2 分钟构建；检查第 3 步 Source 是否 `main` / `/(root)`                              |
| 样式丢了（纯文字页面）                             | 检查 `assets/css/style.css` 是否在仓库根目录正确路径                                       |
| `git commit` 报没有身份                      | 回到第 2 步检查 user.name / user.email 是否设置成功                                      |
| push 反复要密码                              | 用 PAT 登录过一次后会自动记住；或改用 `gh auth login`                                        |

---

## 部署完成后（可选）

- 这份 `部署指南.md` 会一起被推上仓库（Jekyll 会把它渲染成一个页面，无害）。不想要的话，部署成功后本地删掉它再 push 一次即可。
- 域名实名通过后，回到我之前的《阿里云域名注册指南.md》第 6 节做换绑（CNAME + DNS + Custom domain），站点内容零改动。
